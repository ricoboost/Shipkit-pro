import { PageBuilder } from '@/components/waitlist-builder';

export const metadata = {
  title: 'Waitlist Page Builder',
  description: 'Build your waitlist landing page',
};

export default function WaitlistEditorPage() {
  return <PageBuilder />;
}
