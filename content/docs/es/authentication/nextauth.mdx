---
title: NextAuth.js
description: Configura NextAuth.js v5 para autenticación en ShipKit.
---

# Configuración de NextAuth.js

NextAuth.js es una solución de autenticación flexible que admite proveedores OAuth, credenciales y enlaces mágicos.

## Configuración

Establece el proveedor de autenticación en tu `.env`:

```bash
AUTH_PROVIDER="nextauth"
AUTH_SECRET="your-secret-key-generate-with-openssl"
NEXTAUTH_URL="http://localhost:3000"
```

Genera un secreto:

```bash
openssl rand -base64 32
```

## Proveedores OAuth

### Google

1. Ve a [Google Cloud Console](https://console.cloud.google.com/)
2. Crea un nuevo proyecto o selecciona uno existente
3. Habilita Google+ API
4. Crea credenciales OAuth 2.0
5. Agrega URI de redireccionamiento autorizado: `http://localhost:3000/api/auth/callback/google`

```bash
GOOGLE_CLIENT_ID="your-client-id"
GOOGLE_CLIENT_SECRET="your-client-secret"
```

### GitHub

1. Ve a [Configuración de Desarrollador de GitHub](https://github.com/settings/developers)
2. Crea una nueva aplicación OAuth
3. Establece la URL de devolución de llamada: `http://localhost:3000/api/auth/callback/github`

```bash
GITHUB_CLIENT_ID="your-client-id"
GITHUB_CLIENT_SECRET="your-client-secret"
```

## Detalles de Implementación

La configuración de NextAuth está en `src/lib/auth/nextauth.ts`:

```typescript
import NextAuth from 'next-auth';
import { PrismaAdapter } from '@auth/prisma-adapter';
import Google from 'next-auth/providers/google';
import GitHub from 'next-auth/providers/github';
import Credentials from 'next-auth/providers/credentials';
import { db } from '@/lib/db';

export const { handlers, auth, signIn, signOut } = NextAuth({
  adapter: PrismaAdapter(db),
  providers: [
    Google({
      clientId: process.env.GOOGLE_CLIENT_ID!,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET!,
    }),
    GitHub({
      clientId: process.env.GITHUB_CLIENT_ID!,
      clientSecret: process.env.GITHUB_CLIENT_SECRET!,
    }),
    Credentials({
      // Email/password authentication
    }),
  ],
  callbacks: {
    session({ session, user }) {
      session.user.id = user.id;
      return session;
    },
  },
});
```

## Autenticación con Credenciales

Habilita el inicio de sesión con correo electrónico/contraseña:

```typescript
Credentials({
  credentials: {
    email: { label: 'Email', type: 'email' },
    password: { label: 'Password', type: 'password' },
  },
  async authorize(credentials) {
    const user = await db.user.findUnique({
      where: { email: credentials.email },
    });

    if (!user || !user.password) return null;

    const isValid = await bcrypt.compare(
      credentials.password,
      user.password
    );

    if (!isValid) return null;

    return user;
  },
})
```

## Gestión de Sesión

Obtén la sesión actual:

```typescript
import { auth } from '@/lib/auth';

// Server Component
const session = await auth();

// Client Component
import { useSession } from 'next-auth/react';
const { data: session } = useSession();
```

## Rutas Protegidas

Usa middleware para proteger rutas:

```typescript
// middleware.ts
export { auth as middleware } from '@/lib/auth';

export const config = {
  matcher: ['/dashboard/:path*', '/settings/:path*'],
};
```
