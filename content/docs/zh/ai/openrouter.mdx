---
title: OpenRouter
description: 通过 OpenRouter 访问 100 多个 AI 模型。
---

# OpenRouter 设置

OpenRouter 通过单个 API 提供对多个 AI 模型的访问。

## 配置

```bash
AI_PROVIDER="openrouter"
OPENROUTER_API_KEY="sk-or-xxx"
```

## 获取 API 密钥

1. 在 [openrouter.ai](https://openrouter.ai) 创建账户
2. 进入 Keys
3. 创建新的 API 密钥

## 可用模型

OpenRouter 提供访问：

- **OpenAI**: GPT-4、GPT-4 Turbo、GPT-3.5
- **Anthropic**: Claude 3 Opus、Sonnet、Haiku
- **Meta**: Llama 3 70B、8B
- **Mistral**: Mistral Large、Medium、7B
- **Google**: Gemini Pro、PaLM 2
- 以及 100 多个其他模型

## 实现

```typescript
// src/lib/ai/openrouter.ts
export const openRouterProvider: AIProvider = {
  async chat(messages, options = {}) {
    const response = await fetch('https://openrouter.ai/api/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${process.env.OPENROUTER_API_KEY}`,
        'Content-Type': 'application/json',
        'HTTP-Referer': process.env.NEXT_PUBLIC_APP_URL,
      },
      body: JSON.stringify({
        model: options.model || 'openai/gpt-4-turbo',
        messages,
        temperature: options.temperature ?? 0.7,
        max_tokens: options.maxTokens,
        stream: false,
      }),
    });

    const data = await response.json();
    return {
      content: data.choices[0].message.content,
      usage: data.usage,
    };
  },

  async *stream(messages, options = {}) {
    const response = await fetch('https://openrouter.ai/api/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${process.env.OPENROUTER_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        model: options.model || 'openai/gpt-4-turbo',
        messages,
        stream: true,
      }),
    });

    const reader = response.body!.getReader();
    const decoder = new TextDecoder();

    while (true) {
      const { done, value } = await reader.read();
      if (done) break;

      const chunk = decoder.decode(value);
      const lines = chunk.split('\n').filter(line => line.startsWith('data: '));

      for (const line of lines) {
        const data = JSON.parse(line.slice(6));
        if (data.choices[0]?.delta?.content) {
          yield data.choices[0].delta.content;
        }
      }
    }
  },
};
```

## 模型定价

OpenRouter 使用按令牌计费。查看 [openrouter.ai/models](https://openrouter.ai/models) 了解当前价格。

## 成本追踪

```typescript
// 在您的应用中追踪使用情况
const response = await ai.chat(messages);

await db.aiUsage.create({
  data: {
    userId: session.user.id,
    model: 'gpt-4-turbo',
    promptTokens: response.usage.prompt_tokens,
    completionTokens: response.usage.completion_tokens,
    cost: calculateCost(response.usage),
  },
});
```
