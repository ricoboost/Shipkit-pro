---
title: Vercel AI SDK
description: ストリーミングサポート付きのエッジ対応AI。
---

# Vercel AI SDK セットアップ

Vercel AI SDKは、複数のプロバイダーでエッジ対応のストリーミングを提供します。

## 設定

```bash
AI_PROVIDER="vercel"

# プロバイダーを選択
OPENAI_API_KEY="sk-xxx"
ANTHROPIC_API_KEY="sk-ant-xxx"
GOOGLE_AI_API_KEY="xxx"
```

## 実装

```typescript
// src/lib/ai/vercel.ts
import { openai } from '@ai-sdk/openai';
import { anthropic } from '@ai-sdk/anthropic';
import { google } from '@ai-sdk/google';
import { generateText, streamText } from 'ai';

const models = {
  'gpt-4-turbo': openai('gpt-4-turbo'),
  'claude-3-opus': anthropic('claude-3-opus-20240229'),
  'gemini-pro': google('gemini-pro'),
};

export const vercelProvider: AIProvider = {
  async chat(messages, options = {}) {
    const model = models[options.model || 'gpt-4-turbo'];

    const { text, usage } = await generateText({
      model,
      messages,
      temperature: options.temperature,
      maxTokens: options.maxTokens,
    });

    return { content: text, usage };
  },

  async *stream(messages, options = {}) {
    const model = models[options.model || 'gpt-4-turbo'];

    const { textStream } = await streamText({
      model,
      messages,
      temperature: options.temperature,
    });

    for await (const chunk of textStream) {
      yield chunk;
    }
  },
};
```

## Reactフック

組み込みフックを使用：

```tsx
import { useChat, useCompletion } from 'ai/react';

function ChatComponent() {
  const { messages, input, handleInputChange, handleSubmit } = useChat({
    api: '/api/ai/chat',
  });

  return (
    <form onSubmit={handleSubmit}>
      {messages.map(m => (
        <div key={m.id}>{m.content}</div>
      ))}
      <input value={input} onChange={handleInputChange} />
    </form>
  );
}
```

## エッジランタイム

最低レイテンシーのためにエッジにデプロイ：

```typescript
// src/app/api/ai/chat/route.ts
export const runtime = 'edge';

import { streamText } from 'ai';
import { openai } from '@ai-sdk/openai';

export async function POST(req: Request) {
  const { messages } = await req.json();

  const result = await streamText({
    model: openai('gpt-4-turbo'),
    messages,
  });

  return result.toDataStreamResponse();
}
```

## ツール呼び出し

関数呼び出しを追加：

```typescript
const result = await generateText({
  model: openai('gpt-4-turbo'),
  messages,
  tools: {
    weather: {
      description: 'Get weather for a location',
      parameters: z.object({
        location: z.string(),
      }),
      execute: async ({ location }) => {
        return fetchWeather(location);
      },
    },
  },
});
```
