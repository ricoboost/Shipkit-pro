---
title: AI統合の概要
description: ShipKitは複数のプロバイダーとの統一されたAI統合を提供します。
---

# AI統合

ShipKitには、ストリーミングサポートと複数のプロバイダーを備えた本番環境対応のAI統合が含まれています。

## サポートされているプロバイダー

<Cards>
  <Card title="OpenRouter" href="/docs/ai/openrouter">
    GPT-4、Claude、Llama、Mistralを含む100以上のモデルにアクセス。
  </Card>
  <Card title="Vercel AI SDK" href="/docs/ai/vercel-ai-sdk">
    OpenAI、Anthropic、Googleなどとのエッジ対応ストリーミング。
  </Card>
</Cards>

## 設定

```bash
AI_PROVIDER="openrouter"  # または "vercel"

# OpenRouter
OPENROUTER_API_KEY="sk-or-xxx"

# Vercel AI SDK / 直接API
OPENAI_API_KEY="sk-xxx"
ANTHROPIC_API_KEY="sk-ant-xxx"
GOOGLE_AI_API_KEY="xxx"
```

## 統一インターフェース

```typescript
// src/lib/ai/types.ts
export interface AIProvider {
  chat(messages: Message[], options?: ChatOptions): Promise<ChatResponse>;
  stream(messages: Message[], options?: ChatOptions): AsyncIterable<string>;
}
```

## 基本的な使用方法

```typescript
import { ai } from '@/lib/ai';

// シンプルなチャット
const response = await ai.chat([
  { role: 'user', content: 'Hello!' }
]);

console.log(response.content);
```

## ストリーミング

```typescript
import { ai } from '@/lib/ai';

// ストリーミングレスポンス
for await (const chunk of ai.stream([
  { role: 'system', content: 'You are a helpful assistant.' },
  { role: 'user', content: 'Tell me a story.' }
])) {
  process.stdout.write(chunk);
}
```

## APIルート

```typescript
// src/app/api/ai/chat/route.ts
import { ai } from '@/lib/ai';
import { auth } from '@/lib/auth';
import { credits } from '@/lib/payments/credits';

export async function POST(req: Request) {
  const session = await auth.getSession();
  if (!session) {
    return new Response('Unauthorized', { status: 401 });
  }

  // クレジットを確認
  const hasCredits = await credits.deduct(session.user.id, 1, 'AI chat');
  if (!hasCredits) {
    return new Response('Insufficient credits', { status: 402 });
  }

  const { messages } = await req.json();

  // ストリーミングレスポンスを返す
  const stream = ai.stream(messages);

  return new Response(
    new ReadableStream({
      async start(controller) {
        for await (const chunk of stream) {
          controller.enqueue(new TextEncoder().encode(chunk));
        }
        controller.close();
      },
    }),
    {
      headers: { 'Content-Type': 'text/event-stream' },
    }
  );
}
```

## クライアントコンポーネント

```tsx
'use client';

import { useState } from 'react';
import { useChat } from '@/hooks/use-chat';

export function ChatInterface() {
  const { messages, input, handleSubmit, isLoading } = useChat();

  return (
    <div>
      <div className="messages">
        {messages.map((m) => (
          <div key={m.id} className={m.role}>
            {m.content}
          </div>
        ))}
      </div>

      <form onSubmit={handleSubmit}>
        <input
          value={input}
          onChange={(e) => setInput(e.target.value)}
          placeholder="Say something..."
        />
        <button disabled={isLoading}>Send</button>
      </form>
    </div>
  );
}
```

## モデルの選択

モデルを簡単に切り替え:

```typescript
const response = await ai.chat(messages, {
  model: 'gpt-4-turbo',  // または 'claude-3-opus', 'llama-3-70b', など
  temperature: 0.7,
  maxTokens: 1000,
});
```

## デモコンポーネント

ShipKitには、すぐに使えるAIデモコンポーネントが含まれています:

- ストリーミング付きチャットインターフェース
- テスト用のAIプレイグラウンド
- モデル比較ツール

ダッシュボードの `/ai-playground` でアクセスできます。
