---
title: OpenRouter
description: Acesse mais de 100 modelos de IA através do OpenRouter.
---

# Configuração do OpenRouter

OpenRouter fornece acesso a múltiplos modelos de IA através de uma única API.

## Configuração

```bash
AI_PROVIDER="openrouter"
OPENROUTER_API_KEY="sk-or-xxx"
```

## Obter Chave de API

1. Criar conta em [openrouter.ai](https://openrouter.ai)
2. Ir para Keys
3. Criar nova chave de API

## Modelos Disponíveis

OpenRouter fornece acesso a:

- **OpenAI**: GPT-4, GPT-4 Turbo, GPT-3.5
- **Anthropic**: Claude 3 Opus, Sonnet, Haiku
- **Meta**: Llama 3 70B, 8B
- **Mistral**: Mistral Large, Medium, 7B
- **Google**: Gemini Pro, PaLM 2
- E mais de 100 modelos adicionais

## Implementação

```typescript
// src/lib/ai/openrouter.ts
export const openRouterProvider: AIProvider = {
  async chat(messages, options = {}) {
    const response = await fetch('https://openrouter.ai/api/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${process.env.OPENROUTER_API_KEY}`,
        'Content-Type': 'application/json',
        'HTTP-Referer': process.env.NEXT_PUBLIC_APP_URL,
      },
      body: JSON.stringify({
        model: options.model || 'openai/gpt-4-turbo',
        messages,
        temperature: options.temperature ?? 0.7,
        max_tokens: options.maxTokens,
        stream: false,
      }),
    });

    const data = await response.json();
    return {
      content: data.choices[0].message.content,
      usage: data.usage,
    };
  },

  async *stream(messages, options = {}) {
    const response = await fetch('https://openrouter.ai/api/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${process.env.OPENROUTER_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        model: options.model || 'openai/gpt-4-turbo',
        messages,
        stream: true,
      }),
    });

    const reader = response.body!.getReader();
    const decoder = new TextDecoder();

    while (true) {
      const { done, value } = await reader.read();
      if (done) break;

      const chunk = decoder.decode(value);
      const lines = chunk.split('\n').filter(line => line.startsWith('data: '));

      for (const line of lines) {
        const data = JSON.parse(line.slice(6));
        if (data.choices[0]?.delta?.content) {
          yield data.choices[0].delta.content;
        }
      }
    }
  },
};
```

## Preços do Modelo

OpenRouter usa preços por token. Consulte [openrouter.ai/models](https://openrouter.ai/models) para preços atuais.

## Rastreamento de Custos

```typescript
// Rastrear uso em sua aplicação
const response = await ai.chat(messages);

await db.aiUsage.create({
  data: {
    userId: session.user.id,
    model: 'gpt-4-turbo',
    promptTokens: response.usage.prompt_tokens,
    completionTokens: response.usage.completion_tokens,
    cost: calculateCost(response.usage),
  },
});
```
